set timeout=0
set default=0

# Real-hardware-safe console setup: force GRUB text console and avoid
# graphics-mode transitions that can leave Multiboot without a console.
terminal_input console
terminal_output console

menuentry "LC(OS)" {
    multiboot2 /boot/lcos.elf
    module2 /boot/fs.img fsimg
    boot
}
